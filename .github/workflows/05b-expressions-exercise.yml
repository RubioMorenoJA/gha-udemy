name: 05b - Expressions exercise
on:
  push:
    branches:
      - main
    paths-ignore:
      - 'README.md'
  workflow_dispatch:
jobs:
    runner-verifying:
        runs-on: ubuntu-latest
        steps:
            - name: SO information
              id: soInfo
              run: |
                echo "SO information:"
                echo "- OS name: ${{ runner.os }}"
                echo "- OS architecture: ${{ runner.arch }}"
            - name: soInfo results
              run: |
                echo "Steps: ${{ toJson(steps) }}"
                echo "Results: ${{ steps.soInfo.outputs.result }}"
